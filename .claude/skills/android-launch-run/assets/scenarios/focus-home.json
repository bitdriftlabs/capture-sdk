{
  "name": "focus-home",
  "description": "Baseline: HOME drops focus and fires ON_STOP, so the focus flush and the lifecycle flush overlap.",
  "rust_log": "info,bd_client_stats=debug,bd_logger=debug,bd_api=debug,bd_runtime=debug,bd_workflows=debug",
  "modes_before_launch": [],
  "steps": [
    {
      "action": "launch"
    },
    {
      "action": "wait",
      "seconds": 8,
      "why": "settle and let one foreground disk flush land as a positive control"
    },
    {
      "action": "home"
    },
    {
      "action": "wait",
      "seconds": 20,
      "why": "focus loss AND ON_STOP both fire here, so both flush paths are visible"
    }
  ],
  "reference_event": "window focus=false"
}
