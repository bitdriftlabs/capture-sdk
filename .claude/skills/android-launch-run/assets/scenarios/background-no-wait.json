{
  "name": "background-no-wait",
  "description": "Background almost immediately after launch, deliberately NOT clearing the 30s stats upload debounce. Diagnostic: on shared-core <= 42637e1f this reliably produced a debounced (never-attempted) upload; if the c3ba1cba rework only gates *periodic* uploads, an explicit flush upload should now go out anyway.",
  "rust_log": "info,bd_client_stats=debug,bd_logger=debug,bd_api=debug",
  "modes_before_launch": [],
  "steps": [
    {"action": "launch"},
    {"action": "wait", "seconds": 5,
     "why": "deliberately far under the 30s minimum upload interval — this is the variable under test, do NOT raise it"},
    {"action": "home"},
    {"action": "wait", "seconds": 40}
  ],
  "reference_event": "process ON_STOP"
}
